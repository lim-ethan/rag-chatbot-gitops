apiVersion: v1
kind: Service
metadata:
  name: rag-chatbot-backend
  namespace: rag-chatbot
spec:
  type: ClusterIP
  selector:
    app: rag-chatbot-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
