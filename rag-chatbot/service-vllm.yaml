apiVersion: v1
kind: Service
metadata:
  name: vllm-qwen
  namespace: rag-chatbot
spec:
  type: ClusterIP
  selector:
    app: vllm-qwen
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
